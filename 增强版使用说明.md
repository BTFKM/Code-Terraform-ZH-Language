# 使用增强版

这是基于 BTFKM/Code-Terraform-ZH-Language 的增强版本。已有脚本目录时，可以只替换 `ctzh.py`，继续使用自己的 `language.json`。完整压缩包则可以解压到新目录使用。

## 运行

需要 Python 3.10 或更高版本。在解压后的目录打开终端：

```powershell
python -m pip install -r requirements.txt
python ctzh.py --workers 32
```

省略 `--workers 32` 也会自动识别可用 CPU 逻辑线程。找不到游戏时可指定完整路径：

```powershell
python ctzh.py --exe "D:\SteamLibrary\steamapps\common\CodeTerraform\code-terraform.exe" --workers 32
```

使用已有的自定义译文：

```powershell
python ctzh.py --lang "D:\我的汉化\language.json" --workers 32
```

仅提前求解、保存缓存：

```powershell
python ctzh.py --workers 32 --cache-only
```

随后去掉 `--cache-only` 即可应用汉化。恢复原版使用 `python ctzh.py --restore`。

## 缓存与中断

每个文件成功后立即写入脚本旁的 `solutions/v2/`。完整压缩数据和校验索引都会落盘，重启后直接读取，已成功的文件无需重新压缩。

发生中断、某个文件失败或稍后再运行时，执行原来的命令即可。缓存按实际内容和目标长度区分，修改译文后也不会误用旧结果。异常或损坏的缓存会自动忽略并重算。

本地交付压缩包包含本次已计算成功的 5 项缓存，可用于本机这份游戏文件和附带译文；其他输入会自动重新计算。

## 加速方式

默认通过 Brotli 格式的元数据直接补齐压缩长度，解压结果保持为完整的已翻译 JS。这样通常只需要少量压缩，就能完成原来需要反复试算的工作。

快速方式并行处理 5 个文件，因此不需要持续占满 32 线程。若需原来的随机注释搜索方式：

```powershell
python ctzh.py --method search --workers 32
```

传统搜索最多使用 32 个原生压缩线程，命中后停止派发并取消排队任务。已在压缩的任务会结束当前调用，成功结果会在等待这些任务结束前写入缓存。

## 本次验证结果

- 环境：本机 32 个逻辑 CPU 线程，Python 3.12.14，Brotli 1.2.0。
- 游戏文件大小：319,938,560 字节，发现 869 个资产、5 个字典宿主。
- 初次求解：4.55 秒；全部命中缓存：0.03 秒。两者均只计求解阶段，不包括扫描、注入、备份和最终写入。没有据此计算相对旧版的加速倍数。
- 21 项自动测试通过，覆盖 6768 组元数据填充往返组合、缓存损坏、强制退出后缓存保留、原子替换失败、逐项保存、传统搜索及整体打包。
- 在真实游戏文件的副本上完成写入验证：大小不变，仅 5 段压缩资产改变，其他全部字节一致，备份与本机原文件 SHA-256 一致。
- 5 个实际缓存文件同时通过 Python Brotli 和 Node.js Brotli 解压内容校验。
- 本次验证使用游戏副本，游戏内界面尚未验收。当前语言包可注入 9036 条；发现 1 条占位符不合规并按原规则跳过，未改动译文本身。

详细算法和原项目说明见 `README.md`。测试命令为 `python -m unittest -v`。
